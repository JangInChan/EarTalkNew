import { StyleSheet } from "react-native";

const DeleteAccountScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 16,
    backgroundColor: '#fff',
  },
  warningText: {
    fontFamily: 'KCC-Hanbit',
    fontSize: 16,
    color: 'red',
    marginBottom: 20,
    textAlign: 'center',
  },
});

export default DeleteAccountScreenStyles;